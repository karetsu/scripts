#!/usr/bin/env zsh

pkill polybar

desktop=$(echo $DESKTOP_SESSION)

case $desktop in
  none+xmonad)
    if ! pgrep -x polybar; then
      polybar xmonad &
      ln -s /tmp/polybar_mqueue.$! /tmp/xmonad-mode
    else
      pkill -USR1 polybar
    fi
    ;;
esac
